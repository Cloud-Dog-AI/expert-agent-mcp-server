# Public Project Rules

This repository is a public source export. Keep examples runnable from the
public files in this repository and do not add internal workspace paths,
private service hostnames, runtime secrets, or credential-bearing URLs.

Use `.env.example`, `docker-env.public.example`, and the documents under
`docs/` as the public contract for local development and API use.
