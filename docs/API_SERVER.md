# API Server

The public API server documentation is maintained in
[API-REFERENCE.md](API-REFERENCE.md).

Start the API server with:

```bash
python start_api_server.py
```

Use `.env.example` for local configuration and keep credentials in your local
environment, not in repository files.
